import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Dark Theme ─────────────────────────────────────────────────────────────
  static const Color darkBackground = Color(0xFF0D0B1E);
  static const Color darkSurface = Color(0xFF1C1A2E);
  static const Color darkBorder = Color(0xFF2E2A50);

  // ── Light Theme ────────────────────────────────────────────────────────────
  static const Color lightBackground = Color(0xFFF5F3FF);
  static const Color lightSurface = Color(0xFFEDE9FE);
  static const Color lightBorder = Color(0xFFD4BFFF);

  // ── Brand / Primary ────────────────────────────────────────────────────────
  static const Color primary = Color(0xFFA78BFA);
  static const Color primaryLight = Color(0xFFCDB4F8);
  static const Color primaryDark = Color(0xFF7C5FC8);

  // ── Logo Gradient ──────────────────────────────────────────────────────────
  static const Color logoGradientStart = Color(0xFFCDB4F8);
  static const Color logoGradientEnd = Color(0xFF9068D4);

  // ── Text ───────────────────────────────────────────────────────────────────
  static const Color textOnDark = Color(0xFFB8A9FF);
  static const Color textSubtleOnDark = Color(0xFF4A4870);
  static const Color textOnLight = Color(0xFF5B21B6);
  static const Color textSubtleOnLight = Color(0xFF8B6FD4);
}
